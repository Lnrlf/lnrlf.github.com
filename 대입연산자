# 2. [ 대입연산자 ( =, += ) ]

# [ = ]
x= 7 # x는 7이 같다 (X), 변수 x에 7의 값을 대입한다 (O)

water = 5 # 변수 water에 5의 값을 대입한다.
name = "삼X수" # 변수 name에 "삼X수"를 대입한다.
print(water, name)

# [ += ]
x = 2 # 변수 x에 2를 대입한다
print(x) # x = 2

x += 1 # x에 1을 더 더한 것. (x += 1 이거랑 x = x+1 은 동일한 명령어다.)
print(x) # x = 3으로 출력된다.

print ("-여기까지가 대입 연산자 끝-")
