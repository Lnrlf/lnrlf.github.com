a=0 #계속 곱하는 용도 (횟수)
b=1 #저장과 출력용도 (곱해지기 위해서 0이 아니어야함)
while 11>a:# 11보다 a가 작다면 돌아가고 아니면 끝
    if 10>a: # 만약 (if) 10이 a보다 크다면? -> 작동 / 아니면 끝
        a=a+1 # 계속 숫자가 증가하기 위해서 (횟수를 세야됨)
        b=b*a # 계속 1씩 더해짐으로써 10까지인데 변수 a 하나만으로는 작동이 불가능해서 b에다 더한것
        # [ex : 1x1=1, 1x2=2, 2x3=6, 6x4=24, 24x5= 120....]
        print(b) # 계속 곱할수록 최종적으로 3628800 x 10 = 3628800이 나옴.
